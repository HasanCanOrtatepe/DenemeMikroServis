server:
  port: 8888



spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/HasanCanOrtatepe/DenemeMikroServis.git
          clone-on-start: true
          search-paths: 'configs/{application}'
          default-label: master










